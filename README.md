# SEKSOV Telegram Bot

Telegram-бот для личной фиксации введения одного препарата у нескольких пользователей. У каждого Telegram-пользователя отдельные партии раствора, история введений и остатки.

## Возможности

- создание новой партии раствора только если нет активной партии;
- фиксация введения через кнопки;
- выбор только разрешённых способов и мест введения;
- просмотр текущего статуса партии;
- просмотр последнего введения с остатком после введения;
- просмотр истории с остатком после каждого введения;
- автоматический расчёт остатка текущей партии.

## Быстрый старт

### Windows / PowerShell

Самый простой способ — запускать готовый файл из корня проекта:

```powershell
.\start.ps1
```

Если PowerShell запрещает запуск скриптов, используйте:

```powershell
powershell -ExecutionPolicy Bypass -File .\start.ps1
```

Также можно запустить через cmd/bat:

```powershell
.\start.bat
```

Скрипт сам создаст `.venv`, установит зависимости из `requirements.txt`, создаст `.env` из `.env.example` при первом запуске и попросит заполнить `BOT_TOKEN` / `AUTHORIZED_TELEGRAM_USER_IDS`.

Важно: команды `source .venv/bin/activate` и `.venv/bin/activate` — это Linux/macOS, в PowerShell они не работают. Для ручного запуска на Windows используйте `.\.venv\Scripts\Activate.ps1`.

### Linux / macOS

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# заполните BOT_TOKEN и AUTHORIZED_TELEGRAM_USER_IDS в .env
python run.py
```

Если проект установлен как пакет через `pip install -e .`, можно запускать и командой `seksov-bot`.

## Настройки

| Переменная | Описание | По умолчанию |
| --- | --- | --- |
| `BOT_TOKEN` | токен Telegram-бота от BotFather | обязательно |
| `DATABASE_PATH` | путь к SQLite-базе | `data/seksov.sqlite3` |
| `STANDARD_DOSE_ML` | стандартный объём одного введения | `1.0` |
| `AUTHORIZED_TELEGRAM_USER_IDS` | необязательный allowlist Telegram ID через запятую; если пусто, бот доступен всем, кто знает токен/username бота | пусто |

## UX

Основные действия доступны с главной клавиатуры:

- `➕ Новая партия`
- `✅ Зафиксировать 1 мл` при стандартном `STANDARD_DOSE_ML=1.0`; если настройка изменена, объём в кнопке меняется автоматически
- `📊 Статус`
- `🏁 Завершить партию`
- `🕘 Последний приём`
- `📜 История`

Для безопасности личных медицинских записей рекомендуется заполнить `AUTHORIZED_TELEGRAM_USER_IDS`, чтобы бот отвечал только вашим пользователям.

После фиксации бот сразу показывает текущий приём, предыдущий приём, способ, место и остаток в текущей партии. Если остаток активной партии меньше стандартного введения, бот завершает эту партию и предлагает создать новую, чтобы пользователь не застрял с неиспользуемым остатком. Активную партию также можно завершить вручную кнопкой `🏁 Завершить партию`; история введений при этом сохраняется.
